rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Emulator-only permissive rules to allow E2E tests
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
