import { expect } from 'chai'
import { describe, it } from 'mocha'

describe('send test push (placeholder)', () => {
  it('should run a dummy assertion', () => {
    expect(true).to.equal(true)
  })
})
