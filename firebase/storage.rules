rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Emulator-only permissive rules for local integration tests
      allow read, write: if true;
    }
  }
}
