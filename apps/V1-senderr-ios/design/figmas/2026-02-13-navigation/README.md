Figma frames + tokens for "Improve MapShell navigation" (2026-02-13)

What’s included
- 4 high‑fidelity SVG frames that match the app colors & typography:
  - `dashboard.svg` — Jobs list + map preview
  - `active-mapshell-locked.svg` — Active job (bottom card locked) with `Back to Jobs`
  - `route-only-browsing.svg` — Route‑only map with job browsing allowed
  - `job-detail-proof.svg` — Job Detail showing proof capture CTA gating
- `tokens.md` — color, typography and spacing tokens to copy into Figma
- Import notes and quick prototype suggestions

How to import into Figma
1. Create a new Figma file.
2. File → Place image... → select the SVGs in this folder.
3. Use `tokens.md` to create Color & Text Styles in Figma.
4. Arrange frames on the canvas and add prototype links if desired.

Want me to (A) export PNGs too, (B) push a branch & open a PR implementing code + design, or (C) generate a clickable Figma prototype JSON? Reply with A, B or C.