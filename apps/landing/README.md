# Landing — Quick Start

Static landing site. Run locally with:

1. `pnpm --filter @gosenderr/web dev` (if applicable) or serve `index.html` in tooling.
2. Deploy via `pnpm deploy:web` or `firebase deploy --only hosting:gosenderr-landing` per `docs/DEPLOYMENT.md`.
