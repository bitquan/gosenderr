# Unified Project Baseline Lock

Date: 2026-02-20
Branch: `V1/unified/local`

## Baseline Decision

`V1-unified-local` is the active baseline worktree for the whole project.

## iOS Baseline (Do Not Lose)

Known-good build currently on device/TestFlight:

- App name: `Senderr`
- Version shown in App Store Connect: `1.0 (6)`
- Status: Uploaded to Apple

Source metadata currently in this worktree:

- Bundle identifier: `com.gosenderr.courier`
- `MARKETING_VERSION`: `1.0`
- `CURRENT_PROJECT_VERSION`: `1`
- Mapbox native token exists in `apps/courieriosnativeclean/ios/Senderrappios/Info.plist`

## Verification Performed

- `pnpm ios:check` passed:
  - Podfile in sync
  - `.xcode.env` in sync
  - `LocalDebug.xcconfig` in sync
  - canonical iOS structure valid

## Baseline Operating Rules

1. Keep this worktree as primary baseline for all active apps.
2. Keep iOS canonical checks green before major merges.
3. Treat App Store Connect build `1.0 (6)` as known-good reference during future iOS updates.
4. For future iOS uploads, increment build number from the current App Store Connect baseline.
